import 'package:flutter/material.dart';

// Dựa trên UI cũ và mới
const kPrimaryColor = Color(0xFF6B4B3E); // Giữ màu nâu
const kBackgroundColor = Color(0xFFFAFAFA); // Nền trắng ngà
const kOffWhiteColor = Color(0xFFF8F8F8); // Màu nền hơi xám cho card/search bar
const kTextColor = Color(0xFF1F1F1F);
const kSecondaryTextColor = Color(0xFF8A8A8A);
const kAccentColor = Color(
  0xFFFFA500,
); // Màu cam cho link/highlight (ví dụ: View All)
const kHeartColor = Color(0xFFFF6464); // Màu đỏ/hồng cho icon heart

// Padding
const double kDefaultPadding = 16.0;
